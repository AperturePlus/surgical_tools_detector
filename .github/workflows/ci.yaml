name: CI/CD

on:
  push:
    branches: ['**']
    tags: ['v*.*.*']
  pull_request:

env:
  ORT_VERSION: "1.24.3"

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  # Build & test on every push / pull-request.
  # On a tag push, also package the binary and upload it as a workflow artifact.
  # ─────────────────────────────────────────────────────────────────────────────
  build:
    name: Build (Windows / MinGW-w64)
    runs-on: windows-latest

    permissions:
      contents: read    # checkout only; no write access needed

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - uses: actions/checkout@v4

      # Install MSYS2 + all MinGW-w64 packages needed to compile the project.
      # pacboy uses the ":p" suffix as shorthand for "mingw-w64-x86_64-".
      - name: Set up MSYS2 / MinGW-w64
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: unzip
          pacboy: >-
            cmake:p
            ninja:p
            opencv:p
            qt6-base:p
            gcc:p
            tools-git:p

      # Download the bundled ONNX Runtime release and generate the MinGW
      # import library (libonnxruntime.a) that CMakeLists.txt expects.
      - name: Download and prepare ONNX Runtime
        run: |
          ORT_ZIP="onnxruntime-win-x64-${ORT_VERSION}.zip"
          curl -fsSL -o "${ORT_ZIP}" \
            "https://github.com/microsoft/onnxruntime/releases/download/v${ORT_VERSION}/${ORT_ZIP}"
          mkdir -p deps
          if command -v unzip >/dev/null 2>&1; then
            unzip -q "${ORT_ZIP}" -d deps/
          else
            tar -xf "${ORT_ZIP}" -C deps/
          fi
          cd "deps/onnxruntime-win-x64-${ORT_VERSION}/lib"
          gendef onnxruntime.dll
          dlltool -d onnxruntime.def -D onnxruntime.dll -l libonnxruntime.a

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build

      # ── Packaging (tag pushes only) ──────────────────────────────────────────
      - name: Package release artifact
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $out = "SGTDetector-$tag-windows-x64"
          New-Item -ItemType Directory -Force $out | Out-Null
          Copy-Item build\SGTDetector.exe        $out\
          Get-ChildItem build\*.dll -ErrorAction SilentlyContinue |
              Copy-Item -Destination $out\
          # Include OpenCV runtime DLLs from the active MinGW toolchain so
          # the packaged exe can run without requiring users to preinstall OpenCV.
          $cache = Get-Content build\CMakeCache.txt
          $compilerLine = $cache | Where-Object { $_ -like 'CMAKE_CXX_COMPILER:FILEPATH=*' } | Select-Object -First 1
          if ($compilerLine) {
              $compiler = ($compilerLine -split '=', 2)[1]
              $mingwBin = Split-Path $compiler -Parent
              Get-ChildItem "$mingwBin\*opencv*.dll" -ErrorAction SilentlyContinue |
                  Copy-Item -Destination $out\
          }
          Copy-Item build\best.onnx              $out\
          Copy-Item build\labels.dict            $out\
          if (Test-Path build\platforms) {
              Copy-Item build\platforms $out\ -Recurse
          }
          Compress-Archive -Path $out -DestinationPath "$out.zip"

      - name: Upload release artifact
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64
          path: SGTDetector-${{ github.ref_name }}-windows-x64.zip

  # ─────────────────────────────────────────────────────────────────────────────
  # Create a GitHub Release and attach the packaged binary.
  # Runs only when the push that triggered this workflow is a version tag.
  # ─────────────────────────────────────────────────────────────────────────────
  release:
    name: Publish GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest

    permissions:
      contents: write   # required to create releases and upload assets

    steps:
      # Use >= 4.1.3 to avoid the arbitrary-file-write vulnerability (GHSA-…)
      - name: Download packaged artifact
        uses: actions/download-artifact@v4.1.8
        with:
          name: windows-x64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: "*.zip"
          generate_release_notes: true
